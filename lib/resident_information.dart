import 'package:flutter/material.dart';

class ResidentInformationPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Resident Information Page'),
      ),
    );
  }
}
